"""DriveSpeedTest backend package.

An honest external-drive profiler: it measures real device throughput (not OS
cache), collects connection/health diagnostics, and produces a verdict on whether
a drive lives up to its marketing claims -- and if not, where the bottleneck is.
"""

__version__ = "1.0.0"
