# DriveSpeedTest backend. Windows-only (uses ctypes + PowerShell for diagnostics).
# The React frontend has its own deps in frontend/package.json.
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
